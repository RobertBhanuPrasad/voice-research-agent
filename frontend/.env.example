# Copy this to .env.local for local development (usually leave VITE_API_URL blank,
# as Vite's proxy in vite.config.js already forwards /api → http://localhost:5000)

# For Vercel production deployment, add this in Vercel → Project → Settings → Environment Variables:
VITE_API_URL=https://voice-research-agent.onrender.com
